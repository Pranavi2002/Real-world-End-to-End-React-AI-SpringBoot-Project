apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
data:
  # Placeholder values for public repo
  # Replace these with actual base64-encoded credentials in your local environment
  username: REPLACE_WITH_BASE64_USERNAME
  password: REPLACE_WITH_BASE64_PASSWORD
  # Replace 'username', 'your_password_here' with your actual username, password, then run:
  # echo -n 'username' | base64
  # echo -n 'your_password_here' | base64